# Backend Placeholder

Do not implement this module yet. Read `PROJECT_PLAN.md` first.

This folder will later contain the backend IoT API. The backend must receive `POST /api/iot/telemetry` from simulator or real ESP32 room nodes and become the single source of truth for dashboard and Discord bot data.
