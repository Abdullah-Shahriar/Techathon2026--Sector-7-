# Discord Bot Placeholder

Do not implement this module yet. Read `PROJECT_PLAN.md` first.

This folder will later contain the Discord bot. The bot must read from backend APIs only and must never import simulator state directly.
